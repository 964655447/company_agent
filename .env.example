# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会上传到仓库）
# 同事拿到代码后，只需在自己的机器上建一个 .env，照此格式填即可。

# ===== Dify 工作流 API Key =====
# 在 Dify 控制台 -> 工作流 -> 访问 API 中获取
DIFY_KEY_WF6=app-替换为你的Dify工作流APIKey

# ===== 数据库（MySQL）=====
# 格式: mysql+aiomysql://<用户名>:<密码>@<主机>:<端口>/<库名>
# 本地默认:
DB_URL=mysql+aiomysql://root:你的数据库密码@127.0.0.1:3306/company_agent
