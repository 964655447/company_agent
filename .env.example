# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会上传到仓库）
# 同事拿到代码后，只需在自己的机器上建一个 .env，照此格式填即可。

# ===== AI 能力 =====
# 2026-08-29 起，所有 AI 分析统一由右下角「智能助手」气泡（/api/chat）承接，
# 将来由单个「集合所有工作流」的统一智能体处理，不再需要 DIFY_* 配置。

# ===== 数据库（MySQL）=====
# 格式: mysql+pymysql://<用户名>:<密码>@<主机>:<端口>/<库名>
# 本地默认（注意驱动是 pymysql，不是 aiomysql）：
DB_URL=mysql+pymysql://root:你的数据库密码@127.0.0.1:3306/company_agent

# 花名册 Excel 路径（可选）：填了且文件存在，运行
#   python backend/setup_mysql.py --seed-employees
# 才会把员工导入 employees 表。留空则跳过，由你自己维护账号。
# 例：ROSTER_XLSX=D:/data/花名册.xlsx
ROSTER_XLSX=

# 演示考勤生成开关（可选）：设为 1 并运行脚本才会随机生成考勤数据
# SEED_ATTENDANCE=1
