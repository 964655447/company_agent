# 公司管理智能体 — 需求文档（PRD）

> 版本：v0.1（初稿）
> 日期：2026-08-27
> 负责人划分：**AI 大脑 = Dify 团队（你们）**；**前端 + 后端 = WorkBuddy（我）**
> 关联材料：`花名册.xlsx`（26 人，5 部门）

---

## 1. 项目背景与目标

公司希望用一个「管理智能体」把 **考勤打卡、费用报销、工资核算、进阶考核** 四件高频人事行政事务集中到对话式入口里，员工用自然语言、管理层用看板，从而降低人工统计与沟通成本。

**目标**
- 员工端：对话式完成打卡、报销、查工资、查岗位知识。
- 管理端：自动生成各类统计报表与异常分析。
- 共有层：基于花名册做身份识别、权限隔离、数据增改查。

**范围边界（本文档最重要的一条）**
- Dify 只承担「AI 大脑」：自然语言理解、工作流编排、知识库检索（RAG）、模型调用。
- 前端 / 后端由本仓库负责：**用户登录与密码、业务数据持久化、按部门/岗位的权限隔离、审批状态机、消息推送**——这些 Dify 不擅长也不该做，全部落在后端。

---

## 2. 用户角色与权限

数据来源：花名册（26 人）。两个视角：**员工端**、**管理端**。

### 2.1 部门与岗位（来自花名册）

| 部门 | 人数 | 岗位层级 |
|------|------|----------|
| 总经办 | 2 | 总经理、总经理助理 |
| 商务部 | 5 | 商务总监、营销经理×2、营销专员×2 |
| 运营部 | 9 | 运营总监、调度经理、调度专员×2、运营经理、运营专员×2、客服经理、客服专员×2 |
| 车队 | 5 | 总车队长、车队长×2、车队文员×2 |
| 财务部 | 4 | 财务总监、会计、出纳、审核 |

### 2.2 权限模型（核心来自花名册「权限」列）

花名册字段：`序号(NO)`、`姓名(Name)`、`工号(ID)`、`登录密码`、`权限(Permissions)`、`岗位(Position)`、`部门(Department)`。**登录账号 = 工号（ID 列，如 220401），不是姓名**。

花名册「权限」字段记录了每个人可访问的范围，例如：
- 冯霞（总经理）：`总经办、商务部、运营部、车队、财务部`（全公司）
- 许梦（营销专员）：`营销专员`（仅本人岗位范围）
- 邓玲（财务总监）：`财务总监、会计、出纳、审核`（财务部全范围）

**结论**：权限 = 「可查看/操作的岗位范围」。后端据此做数据隔离——管理端只能看自己权限范围内的部门/岗位数据，员工端默认只能看自己的数据。

---

## 3. 整体技术架构（三层）

```
┌──────────────────────────────────────────────────────┐
│ 前端层 Frontend                                       │
│  · 员工端（网页/小程序/企微·飞书内嵌聊天窗）             │
│  · 管理端 Dashboard（数据看板）                        │
└───────────────────────┬──────────────────────────────┘
                         │  HTTPS + JWT
┌───────────────────────┴──────────────────────────────┐
│ 后端层 Backend（本仓库负责）                           │
│  · 用户认证（密码加密、登录态）                         │
│  · 业务数据库（考勤/报销/工资/花名册）                  │
│  · 权限中间件（部门/岗位隔离）                          │
│  · 审批状态机 + 通知推送                               │
│  · 调用 Dify API 的适配层                             │
└───────────────────────┬──────────────────────────────┘
                         │  Dify API（HTTP）
┌───────────────────────┴──────────────────────────────┐
│ Dify 层（AI 大脑，你们负责）                             │
│  · 知识库：花名册、职位能力库                            │
│  · 工作流：考勤/报销/工资/考核                           │
│  · LLM 推理 + RAG                                       │
└──────────────────────────────────────────────────────┘
```

**调用关系**：前端只和后端对话；后端在需要「智能」时（识别意图、抽取字段、生成报告、RAG 检索）调用 Dify API，拿回结构化结果后落库并返回前端。

---

## 4. 功能需求（按模块）

每个模块分 **员工端 / 管理端 / 共有逻辑**。

### 4.1 考勤打卡
- **员工端**
  - 输入「打卡」（或指定上下班）→ 后端记录当前时间 → 对比预设上班时间 → 判断是否迟到 → 返回结果。
  - 可查询「我的周报 / 月报」。
- **管理端**
  - 生成指定员工/部门的周度、月度考勤数据。
  - 分析出勤行为（迟到频次、缺勤趋势），输出文字报告（由 Dify 生成，后端取数）。
- **数据**：`attendance` 表。

### 4.2 费用报销
- **员工端**
  - 提交材料（发票/票据图片）→ 后端调 OCR（Dify 视觉 或 外部 OCR API）→ 抽取金额/类目/日期。
  - 缺项提醒（金额缺失、类目未选等）→ 引导补全。
  - 自动路由到对应审批人（按部门/金额阈值）→ 可查询审批进度。
- **管理端**
  - 统计每个员工报销明细。
  - 分析异常报销（同类目金额异常偏高、频次异常），输出风险提示（Dify 生成）。
- **数据**：`reimbursement` 表（含状态机：提交中→审批中→通过/驳回）。

### 4.3 工资核算
- **员工端**
  - 提交绩效达成情况（自然语言或表单）→ 后端调 Dify 抽取结构化绩效 → 结合花名册岗位/部门 → 计算工资条（基本+绩效+补贴）→ 登记入库。
  - 查询本人工资条。
- **管理端**
  - 基于花名册分析各员工工资构成与波动。
  - 稳定性分析（异常涨降）。
- **数据**：`salary` 表。

### 4.4 进阶考核
- **员工端**
  - 输入想了解的岗位 → 后端调 Dify（职位知识库 RAG）→ 返回该岗位介绍、需具备的管理能力/技能 → 助力转岗升职。
- **管理端**
  - 维护职位知识库（在 Dify 知识库维护文档）。
  - 统计员工查询/答题情况（后端记录日志，`assessment_log` 表）。
- **数据**：知识库在 Dify；查询日志在后端。

### 4.5 共有部分
- **身份识别**：输入姓名 + 密码 → 后端校验 → 返回角色/部门/权限 → 决定可进员工端还是管理端。
- **员工端 - 新增数据**：通过对话/表单向各业务表插入记录。
- **管理端 - 查询/维护数据**：对花名册及各业务表做增删改查（受权限约束）。

---

## 5. 数据模型（后端数据库）

> 选型建议：本地部署用 **MySQL/PostgreSQL**；云版用 **Supabase（Postgres）**。以下以逻辑表描述。

### 5.1 employees（花名册，核心）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | PK | 内部主键 |
| no | int | 序号 |
| emp_id | bigint | **工号，唯一登录账号（如 220401）** |
| name | varchar | 姓名 |
| password_hash | varchar | **密码哈希，禁止明文** |
| permissions | json/text | 可访问的岗位范围列表 |
| position | varchar | 岗位 |
| department | varchar | 所属部门 |
| created_at | datetime | |

### 5.2 attendance（考勤）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | PK | |
| employee_id | FK→employees | |
| checkin_time | datetime | 打卡时间 |
| type | enum | 上班/下班 |
| is_late | bool | 是否迟到 |
| work_date | date | 考勤日期 |

### 5.3 reimbursement（报销）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | PK | |
| employee_id | FK | |
| category | varchar | 报销类目 |
| amount | decimal | 金额 |
| ocr_raw | text | OCR 原始识别 |
| status | enum | 提交中/审批中/通过/驳回 |
| approver_id | FK | 审批人 |
| submit_time | datetime | |

### 5.4 salary（工资）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | PK | |
| employee_id | FK | |
| period | varchar | 工资所属月 |
| base | decimal | 基本工资 |
| performance | decimal | 绩效工资 |
| subsidy | decimal | 补贴 |
| total | decimal | 合计 |
| performance_input | text | 绩效达成原始输入 |
| generated_at | datetime | |

### 5.5 assessment_log（考核查询日志）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | PK | |
| employee_id | FK | |
| position_queried | varchar | 查询的岗位 |
| queried_at | datetime | |

---

## 6. 后端 API 设计（草案）

> 所有接口需带 `Authorization: Bearer <JWT>`，由权限中间件校验。

### 认证
- `POST /api/auth/login` `{emp_id, password}` → `{token, role, dept, permissions}`（工号为花名册 ID 列）
- **注册/开户**：账号由花名册批量预置（seed），员工用「工号 + 密码」直接登录，无需自行注册；新员工由管理端「花名册维护」接口新增。

### 员工端
- `POST /api/attendance/checkin` `{type}` → `{is_late, time}`
- `GET  /api/attendance/my?period=month`
- `POST /api/reimbursement/submit` `{files[], category, amount, desc}` → `{missing[], ticket_id}`
- `GET  /api/reimbursement/my`
- `POST /api/salary/submit-performance` `{achievements}` → `{payslip}`
- `GET  /api/salary/my`
- `POST /api/assessment/query` `{position}` → `{intro}`（内部调 Dify）
- `POST /api/data/create` `{entity, fields}`

### 管理端
- `GET  /api/attendance/report?dept=&period=`
- `GET  /api/reimbursement/report`
- `GET  /api/salary/report`
- `GET  /api/assessment/stats`
- `GET/POST/PUT/DELETE /api/roster/*`（花名册维护）
- `GET/POST/PUT/DELETE /api/data/*`（数据维护）

---

## 7. Dify 集成方案（AI 大脑边界）

后端通过一个「Dify 适配层」调用 Dify，约定：

- **调用方式**：Dify `Chatflow` / `Workflow` 的 `/chat-messages` 或 `/workflows/run` API，传入结构化参数，要求返回 **JSON 结构化输出**（用工作流的「直接回复/变量」约束）。
- **职责清单（你们做）**
  - 知识库：花名册导入、职位能力库维护。
  - 工作流：考勤意图识别、报销 OCR+字段抽取、工资绩效→工资条计算、考核 RAG。
  - 报告生成：考勤/报销/工资的分析报告文案。
- **职责清单（后端做）**
  - 把前端请求翻译成 Dify 入参；把 Dify 返回解析落库；权限与登录；推送。

> 约定：Dify 工作流输出必须是可机读的结构（不要只返回一段自然语言），方便后端存储与前端渲染。

---

## 8. 前端规划

- **员工端**：对话式界面（聊天窗）。最低成本方案 = 直接内嵌 **企业微信/飞书** 或 Dify 自带 Web App；如需独立品牌再做网页/小程序。
- **管理端**：数据看板（表格 + 图表），展示考勤/报销/工资报表与异常提示。
- 技术建议（待定）：网页用 React/Vue；小程序按需。

---

## 9. 安全与权限（必须项）

- 密码 **bcrypt/argon2 哈希存储**，花名册原明文密码上线前必须重置并加密。
- JWT 登录态，过期刷新。
- 权限中间件：管理端接口按 `permissions` 过滤部门/岗位数据；员工端接口强制 `employee_id = 当前用户`。
- 敏感操作（改工资、删记录）留审计日志。

---

## 10. 技术选型建议（前后端，待你确认）

| 层 | 选项 A（本地部署） | 选项 B（云版） |
|----|------------------|----------------|
| 后端 | Python FastAPI + MySQL | Python FastAPI + Supabase(Postgres) |
| 前端 | React/Vue 网页 | 同上，或企微/飞书内嵌 |
| 部署 | Docker Compose（G 盘） | 云服务器/Serverless |

> 你们 Dify 走本地还是云版，决定后端数据库选本地 MySQL 还是 Supabase。

### 数据库分阶段策略（已定）

**开发期用 SQLite（零安装，单文件），上线前切换到 MySQL/Postgres**：

- 后端统一走 SQLAlchemy ORM，模型写一次，两种库通吃。
- 开发期：`app.db`（SQLite 文件）即可真实跑通登录/打卡/报销全部业务，无需先搭数据库。
- 上线期：执行 `contracts/database.sql` 建正式库 → 改一行连接配置 → 迁移数据。
- 契约 `database.sql` 始终是**正式库的目标结构**；SQLite 侧由 ORM 自动建表，字段名与契约保持一致。

---

## 11. 实施里程碑

| 阶段 | 内容 | 负责 |
|------|------|------|
| P0 | 需求文档定稿 + 技术选型确认 | 双方 |
| P1 | 后端：认证 + employees 表 + 权限中间件（身份识别可用，SQLite 先行） | 我 |
| P1 | Dify：花名册知识库 + 身份识别工作流 | 你们 |
| P2 | 考勤模块（前端对话 + 后端 API + Dify 迟到判断） | 双方 |
| P3 | 工资核算模块（绩效抽取 + 计算） | 双方 |
| P4 | 进阶考核（RAG） | 你们 + 我接日志 |
| P5 | 费用报销（OCR + 审批流 + 推送） | 双方 |
| P6 | 管理端看板 + 报表 | 我 |

---

## 12. 责任划分（一句话总结）

- **你们（Dify 团队）**：AI 大脑——知识库、工作流、RAG、报告文案。只输出结构化结果。
- **我（WorkBuddy）**：前后端——登录/密码、数据库、权限、审批流、推送、前端界面、Dify 适配层。把 AI 结果落库并呈现给用户。

---

## 13. 契约与变更流程（多人协作防漂移）

为防止多人开发导致字段/接口不一致，一切以 `contracts/` 目录为**唯一事实来源**：

| 文件 | 管什么 |
|------|--------|
| `contracts/database.sql` | 所有表结构、字段名、类型（建库直接执行它） |
| `contracts/openapi.yaml` | 所有后端接口路径、入参、出参 |
| `contracts/dify-io.md` | 每个 Dify 工作流的入参/返回 JSON 约定 |

**变更流程（必须遵守）**：
1. 谁要加/改字段 → **先改契约文件**，不是先改代码。
2. 在 `dify-io.md` 的「变更记录」表登记一行。
3. 群里同步，评审通过后再改各自代码。
4. 冲突仲裁：代码与契约不一致时，**以契约为准改代码**。

---

### 待确认事项（请回复）
1. Dify 本地部署还是云版？（决定数据库选型）
2. 前端用网页、小程序，还是直接内嵌企微/飞书？
3. 工资核算的「基本工资/补贴」基数从哪来？（花名册暂无，需补充或管理端录入）
4. 报销 OCR 用 Dify 视觉还是接外部 OCR API（百度/腾讯）？
